---
name: user-role
description: Developer profile — learning Embedded Swift and WASM runtime internals via iterative macOS-first implementation
metadata:
  type: user
---

The developer is learning both Embedded Swift constraints and WebAssembly runtime internals simultaneously through a real project. They are fluent in Swift but new to both Embedded and WASM. They follow a phased approach: implement correctly on macOS first, then migrate to Embedded constraints later (Phase 5+).

Tailor explanations to build mental models of WHY a constraint exists (e.g., why `String` is unavailable in Embedded Swift, why stack-machine semantics require careful type checking). Assume deep Swift knowledge; skip basic syntax explanations.
